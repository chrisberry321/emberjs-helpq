import { closedTicketCount } from 'qna/helpers/closed-ticket-count';
import { module, test } from 'qunit';

module('Unit | Helper | closed ticket count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = closedTicketCount([42]);
  assert.ok(result);
});
