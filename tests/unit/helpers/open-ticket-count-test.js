import { openTicketCount } from 'qna/helpers/open-ticket-count';
import { module, test } from 'qunit';

module('Unit | Helper | open ticket count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = openTicketCount([42]);
  assert.ok(result);
});
