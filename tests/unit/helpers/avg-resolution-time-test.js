import { avgResolutionTime } from 'qna/helpers/avg-resolution-time';
import { module, test } from 'qunit';

module('Unit | Helper | avg resolution time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avgResolutionTime([42]);
  assert.ok(result);
});
